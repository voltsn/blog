{
  "name": "arno-volts-blog",
  "version": "0.0.1",
  "author": "Arno Volts",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@11.1.0",
  "scripts": {
    "dev:web": "turbo run dev --filter=web",
    "build:web": "turbo run build --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.4.2"
  }
}
